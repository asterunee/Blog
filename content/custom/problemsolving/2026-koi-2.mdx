---
title: KOI 2026 Round 2 – High School Division
description: KOI 2026 Round 2 – High School Division 풀이
category: koi
algorithmTopics: []
tags: []
date: 2026-08-20
updated: 2026-08-20
author: asterunee
showToc: true
featured: false
draft: false
---
## 1. 간식 분배

![](/images/content/problemsolving/2026-koi-2/Screenshot%202026-08-20%20at%2011.28.20%E2%80%AFPM.png)

처음 보면 매칭처럼 보이는데 실제로는 degree 1 peeling으로 끝난다. 어떤 시점에 학생 `i`가 정확히 하나의 간식을 가져가려면 현재 남아 있는 좋아하는 간식의 개수가 정확히 1이어야 한다. 특히 처음 들어가는 학생은 처음부터 degree가 1이어야 하고, 그 학생이 유일하게 좋아하는 간식 `x`를 가져가면 `x`를 삭제한 뒤 `x`를 좋아하던 다른 학생들의 degree를 전부 1씩 줄이면 된다. 새로 degree 1이 된 학생들을 queue에 계속 넣어서 Kahn 비슷하게 긁으면 된다. 이 greedy가 안전한 이유도 간단한데, 현재 degree 1인 학생의 유일한 간식이 `x`라면 어떤 정답 순서에서도 그 학생보다 먼저 `x`를 가져가는 다른 학생이 있을 수 없다. 그러면 이 학생은 나중에 0개를 가져가게 되기 때문에, 그냥 지금 맨 앞에 빼도 항상 손해가 없다. 각 간식에서 그 간식을 좋아하는 학생들의 reverse adjacency만 들고 있으면 되고, 학생의 남은 유일한 간식은 degree와 XOR를 같이 관리하면 set 없이도 바로 찾을 수 있다. 끝까지 `N`명을 전부 제거했으면 제거한 순서가 그대로 답이고 중간에 queue가 비었는데 학생이 남으면 불가능하다. 각 학생-간식 edge는 간식이 사라질 때 딱 한 번만 보므로 전체 복잡도는 $O(N+sum C\[i])$.

## 2. 극솟값 제거

![](/images/content/problemsolving/2026-koi-2/Screenshot%202026-08-20%20at%2011.28.29%E2%80%AFPM.png)

전체 배열에서 각 원소가 몇 번째 변화에서 지워지는지를 먼저 전처리해두고, 쿼리에서는 양쪽 경계 때문에 실제로는 안 지워지는 애들만 보정하는 식으로 간다. 먼저 monotone stack으로 `L[i]`를 왼쪽에서 가장 가까운 `A[j] > A[i]`, `R[i]`를 오른쪽에서 가장 가까운 `A[j] > A[i]`로 잡는다. 전체 배열 기준 제거 시각을 `D[i]`라고 하면 한쪽에 더 큰 원소가 아예 없는 애는 prefix/suffix maximum이라 영원히 안 지워지고, 나머지는 작은 값부터 보면서 `i`가 지워진 다음에야 그 양쪽 nearest greater가 다음 단계 후보가 될 수 있다는 걸 이용해 `D[L[i]] = max(D[L[i]], D[i]+1)`, `D[R[i]] = max(D[R[i]], D[i]+1)` 식으로 올리면 모든 제거 시각을 구할 수 있다. 이제 쿼리 `[l,r]`에서 `D[i] <= t`인 원소를 일단 전부 지워진 걸로 세면 거의 맞는데, `L[i] < l`인 애들은 구간 안에서는 prefix maximum이라 절대 안 지워지고 `R[i] > r`인 애들도 suffix maximum이라 절대 안 지워진다. 이 예외들은 각각 `l -> R[l] -> R[R[l]] -> ...`와 `r -> L[r] -> L[L[r]] -> ...` 체인으로 정확히 잡히고, 체인을 따라갈수록 `D`가 증가하므로 binary lifting으로 `D <= t`인 구간만 빠르게 셀 수 있다. 두 체인은 구간 최댓값에서 한 번 겹치므로 그 중복만 마지막에 한 번 복구하면 된다. 한편 `[l,r]` 안에서 `D[i] <= t`인 전체 개수는 쿼리를 `t`순으로 offline 정렬하고 Fenwick에 `D[i]`가 작은 위치부터 추가해서 바로 구하면 된다. 결국 global하게 지워지는 개수에서 왼쪽 prefix maximum chain과 오른쪽 suffix maximum chain을 빼고 겹친 최댓값 하나를 보정하면 실제 제거 개수가 나오고, 답은 구간 길이에서 그 값을 빼면 된다. 전체 복잡도는 $O((N+Q)log N)$.

## 3. 곡예

![](/images/content/problemsolving/2026-koi-2/Screenshot%202026-08-21%20at%207.24.06%E2%80%AFAM.png)

상태를 `(Alice 위치, Bob 위치)`로 직접 잡으면 상태가 $O(N^2)$이라 불가능하고, 대신 두 사람이 붙어 있는 `(i,i+1)` 상태가 어디까지 확장될 수 있는지만 보면 된다. 걷기는 두 사람 사이의 구간을 줄이는 행동이므로, 어떤 상태에서 한 번 확보한 구간 내부의 `(j,j+1)` 상태는 전부 만들 수 있다. 따라서 각 경계 `i`에 대해 최종적으로 도달 가능한 최대 구간을 `[L[i], R[i]+1]`로 정의한다. 점프 `x -> y`에서 `x < y`라면 `(x-1,x)`에서 Bob이 점프해 오른쪽을 `y`까지 늘릴 수 있고, `x > y`라면 `(x,x+1)`에서 Alice가 점프해 왼쪽을 `y`까지 늘릴 수 있으므로 이것들로 초기 `L, R`을 만든다. 이후 현재 `[L[i],R[i]]` 안에 들어 있는 모든 단위 경계 `j`의 도달 구간을 다시 합치면 `L[i] = min L[j]`, `R[i] = max R[j]`로 한 번 더 확장할 수 있다. 이 갱신은 한 번 할 때마다 이미 갈 수 있는 구간들의 closure를 한 단계 합치는 형태라 $O(log N)$번이면 안정되고, 각 단계의 구간 `min/max`는 세그먼트 트리로 처리한다. 전처리가 끝나면 일반 시작 상태 `(a,b)`는 `a`부터 `b-1`까지의 단위 경계를 모두 포함하므로 그 구간의 `L` 최솟값과 `R` 최댓값만 구하면 되고, 목표 `(c,d)`가 그 최종 구간 안에 들어가는지 확인하면 된다. 전체 복잡도는 전처리 $O(N log^2 N + M)$, 각 쿼리 $O(log N)$이다.

## 4. 공장

![](/images/content/problemsolving/2026-koi-2/Screenshot%202026-08-21%20at%207.24.21%E2%80%AFAM.png)

이 문제는 날짜별로 지원자를 숙련도 `A` 순으로 세운 뒤, 인접한 두 숙련도 사이의 구간을 하나의 영역으로 보는 식으로 바꾸면 min-cut이 나온다. 어떤 지원자를 고용했는지는 그 지원자를 기준으로 위아래 영역의 선택 여부가 바뀌는지로 표현할 수 있고, 따라서 같은 날짜에서 인접 영역의 상태가 다르면 그 경계에 있는 지원자를 고용한 것이 되어 기본 임금 `C[i]`를 비용으로 둔다. 낮 생산 이익은 선택된 영역들의 가중치로 바꿀 수 있는데, 인접 지원자 사이 영역에 `B[next]-B[cur]`를 주면 선택 영역의 합이 실제 짝들의 `B` 차이로 telescoping된다. 밤은 더 자연스럽다. 날짜 `d`와 `d+1`의 영역을 숙련도 축에서 겹쳐 보면, 두 영역의 선택 여부가 다를 때 그 겹친 길이만큼 경계가 생기고 이것이 정확히 해당 밤에 지급되는 `A` 차이의 합이 된다. 따라서 서로 다른 상태가 될 때 비용 `len`이 들도록 두 영역 사이에 양방향 간선을 연결한다. 각 날짜의 숙련도 최하단과 최상단 바깥 영역은 선택되지 않도록 `INF`로 sink에 고정하면 고용 인원이 자동으로 짝수가 되고, 양의 영역 가중치는 source에서, 음의 가중치는 sink로 연결하면 전체 문제는 `선택한 영역 가치 - 경계 비용`을 최대화하는 maximum weight closure가 된다. 최대 유량을 흘린 뒤 source 쪽에 남은 영역들을 선택된 영역으로 보고, 같은 날짜에서 양옆 영역의 cut-side가 다른 지원자들을 고용하면 최적 해까지 바로 복원할 수 있다. 정점과 간선 수는 모두 $O(N+T)$ 수준이라 제한 안에 충분히 들어온다.
