import Link from "next/link";
import { notFound } from "next/navigation";
import { getLogs, getPosts, getSolutions } from "@/lib/content";

export function generateStaticParams() { return [...new Set([...getPosts(false).flatMap((p) => p.tags), ...getSolutions(false).flatMap((p) => p.tags), ...getLogs(false).flatMap((p) => p.tags)])].map((tag) => ({ tag })); }
export default async function TagPage({ params }: { params: Promise<{ tag: string }> }) { const { tag } = await params; const articles = getPosts().filter((p) => p.tags.includes(tag)).map((p) => ({ ...p, href: `/posts/${p.slug}`, kind: p.category })); const solutions = getSolutions().filter((p) => p.tags.includes(tag)).map((p) => ({ ...p, href: `/solutions/${p.slug}`, kind: "PS 풀이" })); const logs = getLogs().filter((p) => p.tags.includes(tag)).map((p) => ({ ...p, href: `/log/${p.slug}`, kind: p.type })); const posts = [...articles,...solutions,...logs].sort((a,b) => b.date.localeCompare(a.date)); if (!posts.length) notFound(); return <div className="page-shell archive-page"><header className="page-title"><span className="section-index">TAG ARCHIVE</span><h1>#{tag}</h1><p>{posts.length}개의 글이 이 태그에 연결되어 있습니다.</p></header><div className="simple-post-list">{posts.map((p) => <Link href={p.href} key={p.href}><span>{p.date} · {p.kind}</span><h2>{p.title}</h2><p>{p.description}</p></Link>)}</div></div>; }
